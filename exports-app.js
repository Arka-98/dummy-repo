const { name2, name3, data: { arr: [a, b, ...c], obj: { name, company }, str } } = require("./names")
const sayHi = require("./utils")

// console.log(info)
console.log(c)
// sayHi(name2)
// sayHi(name3)